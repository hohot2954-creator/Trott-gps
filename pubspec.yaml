name: trott_gps
description: Application GPS Trottinette
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.3  # <- Ajout de la carte Google Maps

flutter:
  uses-material-design: true
name: trott_gps
description: Application GPS Trottinette
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0

flutter:
  uses-material-design: true
name: trott_gps
description: Application GPS Trottinette
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0

flutter:
  uses-material-design: true
dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0
  flutter_tts: ^3.8.5  # <- Pour que l'application puisse te parler
