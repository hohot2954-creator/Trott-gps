      - name: Force Android v2 scaffolding
        run: |
          flutter create . --platforms=android --org com.example.trottgps
          # On crée ou force le fichier local.properties et le bon embedding
          mkdir -p android/app/src/main
          echo "flutter.embedding.version=2" >> android/local.properties || true
